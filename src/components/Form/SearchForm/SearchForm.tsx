import React from "react";
import { FC } from "react";
import { SearchFormContainer } from "../FormContainers/SearchFormContainer";

export const SearchForm: FC = () => {
  return (
    <div className="search-form">
      <SearchFormContainer />
    </div>
  );
};
